%h1= @user.first_name + ' ' + @user.last_name
%p The person info is
Name :
= @user.first_name + ' ' + @user.last_name

%p Email Address :
= @user.email

%p Books issued :
- @user.current_issued_books.each do |book|
  = book.title
= button_to 'Back', user_profile_path(current_user), method: :get, class: 'red-button'
