%h1 List of all the books is

%table{ border: '20' }
  %th Title
  %th Author
  %th Publisher
  %th Price
  %th Status
  - @books.each do |book|
    %tr
      %td
        = link_to book.title, book_path(book.id)
      %td
        =book.author
      %td
        =book.publisher
      %td
        =book.price
      %td
        - if book.checked_out?
          Checked out by
          = book.user.first_name + ' ' + book.user.last_name
        - else
          = button_to 'Checkout', checkout_path(id: book.id), confirm: 'Are you sure?'
